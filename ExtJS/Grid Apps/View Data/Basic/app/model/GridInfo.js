Ext.define('DataGrid.model.GridInfo', {
    extend: 'Ext.panel.Panel',
    xtype: 'gridInfo',
    title: 'Grid Info',
    itemId: 'DataGrid-GridInfo',
    bodyPadding: 10,
    // buttons: [{
    // 	text: "Call onClick",
    // 	handler: function() {
    // 		// Calls onclick
    // 		// Doesn't work since parameters are not entered in
    // 		console.log(this.up('#DataGrid').down('#DataGrid-MyGrid').getController().onClick());
    // 	}
    // }]
});